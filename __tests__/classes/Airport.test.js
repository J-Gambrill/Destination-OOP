const Airport = require('./classes/Airport.js')
const { describe, it, expect } = require('@jest/globals')


describe('Person', () => {
    
    const p1 = new Person(100, 1234, 'James')
    
    test('Is an instance created?', () => {
        expect(b1).toBeInstanceOf(Bag)
    })


})
